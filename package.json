{
	"name": "@donutteam/fritter-render-component-middleware",
	"version": "1.0.0",
	"description": "A middleware for the Fritter web framework for rendering Donut Team's Document Builder components.",
	"main": "./build/index.js",
	"scripts": {
		"build": "npx rimraf build && npx tsc",
		"build:watch": "npx rimraf build && npx tsc --watch",
		"prepublishOnly": "npm run build"
	},
	"keywords": [
		"fritter",
		"middleware"
	],
	"author": "Donut Team",
	"license": "MIT",
	"type": "module",
	"peerDependencies": {
		"@donutteam/document-builder": "^8.1.0",
		"@donutteam/fritter": "^4.2.3"
	},
	"devDependencies": {
		"@donutteam/typescript-config": "^2.0.0",
		"rimraf": "^6.0.1",
		"typescript": "^5.7.3"
	}
}
